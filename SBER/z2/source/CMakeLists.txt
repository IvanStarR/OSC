set(MODULE_NAME core)
module(NAME ${MODULE_NAME} TYPE STATIC
    DEPENDENCIES spdlog::spdlog fmt::fmt ${XXHASH_TARGET}
    $<$<BOOL:${URING_TARGET}>:${URING_TARGET}>
    INCLUDE_DIRECTORIES ${CMAKE_SOURCE_DIR}/include)
